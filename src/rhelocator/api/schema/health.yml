---
definitions:
  type: object
  properties:
    message:
      type: string

responses:
  200:
    description: 
    schema:
      $ref: '#/definitions'
    examples:
          {
            "message": "ok"
          }
  500:
    description:
    schema:
      $ref: '#/definitions'
    examples:
          {
            "message": "?"
          }

